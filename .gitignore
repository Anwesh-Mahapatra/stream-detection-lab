# machine-specific, generated by scripts/bootstrap.sh - contains a Kafka cluster ID
# tied to this box's on-disk volume, not something to share or commit
docker/.env

__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# fluent-bit position DB, if ever created inside the repo instead of /var/lib
*.db

.DS_Store
